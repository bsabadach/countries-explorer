# REACTIVE ARCHITECTURE WITHOUT FRAMEWORK

countries explorer app using a JSX version of virtual dom and designed with the State Action Model (sam.js.org) design pattern

usage:
* `yarn add gobal parcel `
* `yarn install`
* `parcel index.html`